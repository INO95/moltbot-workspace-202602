# bot-d Runbook (Reserved)

## Status
- This worker is reserved and inactive in MVP.
- Mapping is defined in `ops/config/daily_ops_mvp.json` with `active=false`.

## Activation Checklist
1. Provision runtime profile/container.
2. Assign dedicated log path (`logs/bot-d/`).
3. Enable worker in config and validate heartbeat + latest updates.
